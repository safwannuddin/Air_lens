services:
  - type: web
    name: eventpulse-mcp
    env: node
    buildCommand: npm install
    startCommand: npm run mcp
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000